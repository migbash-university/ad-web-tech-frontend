const fetch = process.browser ? window.fetch : require('node-fetch').default;

// Function for Internal MiddleWare Post Request:
export function post(endpoint, data) {
	return fetch(endpoint, {
		method: 'POST',
		credentials: 'include',
		body: JSON.stringify(data),
		headers: {
			'Content-Type': 'application/json',
		}
	}).then(r => r.json());
}

// Function for FetchAPI and ability for Pre-flight check
export function post_non_auth(endpoint, data) {
	return fetch(endpoint, {
		method: 'POST',
		body: JSON.stringify(data),
		headers: {
			'Content-Type': 'application/json',
		}
	}).then(r => r.json());
}

// Function for FetchAPI and ability for Pre-flight check
export function get_non_auth(endpoint, data) {
	return fetch(endpoint, {
		method: 'GET',
		body: JSON.stringify(data),
		headers: {
			'Content-Type': 'application/json',
		}
	}).then(r => r.json());
}